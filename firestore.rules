rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Helper to check if user is logged in
    function isAuthenticated() {
      return request.auth != null;
    }
    
    // User profile rules: Authenticated users can read/write their own or others profiles (needed for broker/client lookups)
    match /users/{userId} {
      allow read, write: if isAuthenticated();
    }
    
    // Clients rules: Authenticated users can read/write clients
    match /clients/{clientId} {
      allow read, write: if isAuthenticated();
    }
    
    // Commissions rules: Authenticated users can read/write commissions
    match /commissions/{commissionId} {
      allow read, write: if isAuthenticated();
    }
    
    // Dossier leads rules: Authenticated users can read/write leads
    match /dossier_leads/{leadId} {
      allow read, write: if isAuthenticated();
    }
  }
}
